# common variables for river config

# Use the "logo" key as the primary modifier
mod="Mod4"
term="alacritty --socket '${XDG_RUNTIME_DIR}/alacritty.sock' msg create-window"
term="alacritty"
dropdown_class=floating_term
